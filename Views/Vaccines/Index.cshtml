@model IEnumerable<VaccineAPI2.Models.Vaccine>

@{
    ViewBag.Title = "Vaccines";
}

<h2>Vaccines</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VaccineName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OriginCountry)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VaccineName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OriginCountry)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VaccineID }) |
            @Html.ActionLink("Details", "Details", new { id=item.VaccineID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VaccineID })
        </td>
    </tr>
}

</table>
